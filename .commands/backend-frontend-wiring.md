# Backend + frontend wiring steps

- Added Spring MVC view controller to serve `index.html` from `src/main/resources/templates`.
- Copied Thymeleaf templates and fragments into `src/main/resources/templates/`.
- Copied CSS/JS assets into `src/main/resources/static/` and wired chat JS to call `/api/chat` endpoints.
