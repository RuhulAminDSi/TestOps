{
  "skills": [
    {
      "name": "test",
      "description": "Run Maven tests for the project",
      "command": "mvn test"
    },
    {
      "name": "build",
      "description": "Build the project with Maven",
      "command": "mvn clean package"
    },
    {
      "name": "run",
      "description": "Start the Spring Boot application",
      "command": "mvn spring-boot:run"
    },
    {
      "name": "db-start",
      "description": "Start PostgreSQL database container",
      "command": "docker-compose up -d"
    },
    {
      "name": "db-stop",
      "description": "Stop PostgreSQL database container",
      "command": "docker-compose down"
    },
    {
      "name": "playwright-install",
      "description": "Install Playwright browsers",
      "command": "mvn exec:java -Dexec.mainClass=\"com.microsoft.playwright.CLI\" -Dexec.args=\"install\""
    },
    {
      "name": "sandbox-start",
      "description": "Start Playwright sandbox execution service",
      "command": "cd playwright-runner && npm start"
    }
  ],
  "agents": {
    "codeReview": {
      "description": "Review code changes for bugs, security, and performance issues",
      "model": "claude-sonnet-4-6",
      "tools": ["Read", "Grep", "Bash"]
    },
    "exploration": {
      "description": "Explore and analyze the codebase structure",
      "model": "claude-haiku-4-5-20251001",
      "tools": ["Glob", "Grep", "Read"]
    }
  }
}